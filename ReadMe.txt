## Instructions

Follow these steps to set up the application:

1. Change the default connection string in `appsettings.json` with your own connection string.
2. Run the migration command: Add-Migration <name_of_migration>
3. Update the database with the command:Update-Database
4. Run the backend application.
5. In terminal run cd ticket-management
6. Finally, run the frontend application using:ng serve
